{% extends 'base.html.twig' %}

{% block title %}TodoList{% endblock %}

{% block body %}
    <div class="row">
        <div class="col">
            <h1>TodoList</h1>
        </div>
        <div class="col-2">
            <div class="btn-group">
                <a class="btn btn-outline-secondary" href="{{ path('todo_list_index') }}">Back to list</a>
                <a class="btn btn-outline-info" href="{{ path('todo_list_edit', {'id': todo_list.id}) }}">Edit</a>
            </div>
        </div>
            <div class="col-2 text-right">
        {{ include('todo_list/_delete_form.html.twig') }}
            </div>
    </div>
    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ todo_list.id }}</td>
        </tr>
        <tr>
            <th>Name</th>
            <td>{{ todo_list.name }}</td>
        </tr>
        </tbody>
    </table>

    {% include 'task/_list.html.twig' %}




{% endblock %}
